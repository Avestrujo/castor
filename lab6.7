8
using System.Collections.Generic;
using UnityEngine;

public class ocho : MonoBehaviour
{
    List<string> elementos = new List<string>();

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        elementos.Add("paralelepipedo");
        elementos.Add("parangaricutirimicuaro");
        elementos.Add("desoxirribonucleico");
        elementos.Add("hipopotomonstrosesquipedalofobia");

        print("elemento 1:" + elementos[1]);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
